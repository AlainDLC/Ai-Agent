const config = {
  plugins: ["@tailwindcss/postcss"],
  theme: {
    extend: {
      colors: {
        primary: "#2323FF", // Här anger du din primära färg
      },
    },
  },
};
export default config;
